name: CI

on:
  push:
    branches: 
    - '**'
    tags-ignore: 
    - '**'
    paths-ignore:
      - '.github/*'
      - '.github/workflows/**.yml'
      - '.gitattributes'
      - '.gitignore'
      - 'docs/**'
      - '**.md'
      - 'LICENSE'
  workflow_dispatch:

jobs:
  build-plugin:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Restore packages
        run: dotnet restore Plugin/FEZ.HAT.mm.csproj

      - name: Build
        run: dotnet build Plugin/FEZ.HAT.mm.csproj -c Debug

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HAT
          path: Plugin/bin/Debug/
          if-no-files-found: error

  build-installer:
    strategy:
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
            artifact: HATinstaller-win-x64.exe
          - os: ubuntu-latest
            rid: linux-x64
            artifact: HATinstaller-linux-x64
          - os: macos-latest
            rid: osx-x64
            artifact: HATinstaller-osx-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Publish installer
        run: dotnet publish Installer/FEZ.HAT.cli.csproj -c Release -r ${{ matrix.rid }}

      - name: Rename artifact
        shell: bash
        run: |
          src=$(find Installer/bin/Release -name 'HATinstaller' -o -name 'HATinstaller.exe' | head -1)
          mv "$src" "${{ matrix.artifact }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}
          if-no-files-found: error
